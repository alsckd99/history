# 🎬 실시간 GraphRAG Viewer - Frontend

## 설치

```bash
npm install
```

## 개발 서버 실행

```bash
npm run dev
```

브라우저에서 `http://localhost:5173` 접속

## 빌드

```bash
npm run build
```

빌드된 파일은 `dist/` 폴더에 생성됩니다.

## 환경변수

`.env` 파일 생성:

```bash
VITE_API_BASE=http://localhost:8080
```

## 주요 기능

### 1. 영상 업로드
- 영상 파일 + 키워드 JSON 업로드
- 자동으로 레지스트리에 등록

### 2. 실시간 그래프
- ReactFlow 기반 인터랙티브 그래프
- 영상 재생 시 자동으로 노드 추가
- 드래그, 줌, 팬 지원

### 3. 노드 클릭
- 노드 클릭 시 관련 문서 자동 표시
- 엔티티 상세 정보 확인

### 4. GraphRAG 질문
- 자연어 질문 → AI 답변
- 지식 그래프 기반 답변 생성

## 의존성

- `react` v18.3.1
- `reactflow` v11.11.0
- `@tanstack/react-query` v5.56.2
- `vite` v5.4.2

## 파일 구조

```
src/
├── App.tsx          # 메인 앱 컴포넌트
├── GraphView.tsx    # 그래프 시각화
├── api.ts           # API 클라이언트
├── styles.css       # 글로벌 스타일
└── main.tsx         # 엔트리 포인트
```

## API 엔드포인트

| 메서드 | 경로 | 설명 |
|--------|------|------|
| POST | `/videos/upload` | 영상 업로드 |
| GET | `/videos/{id}/keywords` | 키워드 조회 |
| GET | `/videos/{id}/stream-keywords` | 실시간 스트리밍 (SSE) |
| GET | `/entity/{name}` | 엔티티 상세 |
| POST | `/query` | GraphRAG 질문 |

